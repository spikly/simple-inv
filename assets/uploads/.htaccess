# Uploaded files are data, never code. Apache only; on nginx add the
# equivalent "location ^~ /assets/uploads/ { }" rule with no PHP handler.
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
</IfModule>
